- name: Authorize public key pulled from github for user {{ github_username }}
  ansible.posix.authorized_key:
    user: "{{ github_username }}"
    state: present
    key: "https://github.com/{{ github_username }}.keys"
    manage_dir: true
  become: true