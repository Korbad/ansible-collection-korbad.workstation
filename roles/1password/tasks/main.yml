---
- name: Ensure 1password is installed on Arch
  include_tasks: arch.yml
  when: ansible_distribution == 'Archlinux'

- name: Ensure 1password is installed on AlmaLinux
  include_tasks: alma.yml
  when: ansible_distribution == 'AlmaLinux'
