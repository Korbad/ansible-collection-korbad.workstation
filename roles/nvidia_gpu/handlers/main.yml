- name: modprobe nvidia
  command: ls # modprobe nvidia
  become: true

- name: generate nvidia xorg config
  command: nvidia-xconfig
  become: true

- name: rebuild initramfs
  command: mkinitcpio -P
  become: true