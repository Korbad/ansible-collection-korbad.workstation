- name: Ensure virtualenv is installed (Arch)
  include_role:
    name: korbad.arch.virtualenv
  when: ansible_distribution == 'Archlinux'

- name: Ensure virtualenv is installed (Ubuntu)
  include_role:
    name: korbad.ubuntu.virtualenv
  when: ansible_distribution == 'Ubuntu'
