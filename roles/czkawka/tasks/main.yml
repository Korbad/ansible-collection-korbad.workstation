---
- name: Ensure the czkawka packages are installed
  community.general.cargo:
    name:
      - czkawka_gui
      - czkawka_cli
  environment:
    PATH: "{{ ansible_env.HOME }}/.cargo/bin:{{ ansible_env.PATH }}"