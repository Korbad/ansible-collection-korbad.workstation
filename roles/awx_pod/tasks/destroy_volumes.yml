---
- name: Remove awx_data_volume
  containers.podman.podman_volume:
    name: awx_data_volume
    state: absent

- name: Remove awx_conf_volume
  containers.podman.podman_volume:
    name: awx_conf_volume
    state: absent

- name: Remove awx_settings_volume
  containers.podman.podman_volume:
    name: awx_settings_volume
    state: absent

- name: Remove awx_nginx_html_volume
  containers.podman.podman_volume:
    name: awx_nginx_html_volume
    state: absent

- name: Remove awx_nginx_conf_volume
  containers.podman.podman_volume:
    name: awx_nginx_conf_volume
    state: absent

- name: Remove awx_postgres_volume
  containers.podman.podman_volume:
    name: awx_postgres_volume
    state: absent