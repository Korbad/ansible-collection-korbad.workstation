- debug:
    var: org_item

- name: Apply org defaults
  include_tasks: organization.yml
  vars:
    org_item_defaults:
      value: "{{ awx_configuration.organization_defaults }}"
    org_item_with_defaults: "{{ org_item_defaults | combine(org_item, recursive=True) }}"
